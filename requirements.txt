# ─── Django Core ────────────────────────────────────────────
Django>=4.2,<5.0
djangorestframework>=3.14
python-dotenv>=1.0.1

# ─── Database ───────────────────────────────────────────────
psycopg2-binary>=2.9.9        # PostgreSQL driver (Supabase)

# ─── Server & Static Files ──────────────────────────────────
gunicorn>=21.2.0
whitenoise>=6.7.0             # Static file serving on Render

# ─── PDF Generation ─────────────────────────────────────────
reportlab>=4.1.0
xhtml2pdf
pymupdf

# ─── PDF Extraction (AI upload feature) ─────────────────────
pdfplumber>=0.10.0            # Text-based PDF extraction
pdf2image>=1.17.0             # Scanned PDF → image (needs poppler on server)

# ─── Document Extraction ────────────────────────────────────
python-docx>=1.1.0            # .docx extraction
openpyxl>=3.1.0               # .xlsx extraction
odfpy>=1.4.1                  # .odt extraction
pandas>=2.0.0                 # .xls / fallback spreadsheet extraction

# ─── Image Processing & OCR ─────────────────────────────────
Pillow>=10.2.0
opencv-python-headless        # headless build — no GUI deps on server
numpy>=1.26.4

# ─── HTTP Requests (OCR.Space API) ──────────────────────────
requests>=2.31.0

# ─── AI / LLM ───────────────────────────────────────────────
groq

# ─── String Matching ────────────────────────────────────────
rapidfuzz

# ─── Date & Time Utilities ──────────────────────────────────
python-dateutil>=2.8

# ─── SMS / Communication ────────────────────────────────────
twilio>=9.0.0